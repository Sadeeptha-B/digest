# Copy to `.env` and fill in. The OAuth Client ID is public app metadata (not a secret) and is
# restricted by the authorized JavaScript origins you set in Google Cloud. With this set, users
# don't need to paste a Client ID — they just sign in.
VITE_GOOGLE_CLIENT_ID=
